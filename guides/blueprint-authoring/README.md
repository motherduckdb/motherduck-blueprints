# Blueprint Authoring Guide

This validation-only Guide demonstrates a complete package below the typed `guides/` root. Set `deploy: true` when the repository should publish it; its preview title is already branch-scoped.

See [Manage Guides as code](../../docs/guides-as-code.md) for deployment, access, and reference options.
